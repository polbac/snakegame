import * as React from 'react';

export default class Snake extends React.Component<{}> {
    render() {
        return (
            <main>
                Snake
            </main>
        );
    }   
}